/**
 * Mapbox public token configuration for GitHub Pages.
 *
 * 1. Copy this file to config.js in the same directory:
 *    cp airport-prototype/config.example.js airport-prototype/config.js
 *
 * 2. Replace YOUR_PUBLIC_TOKEN with your Mapbox public token (starts with pk.)
 *
 * 3. Do NOT commit config.js — it is gitignored.
 *
 * For local Vite dev, you can alternatively set VITE_MAPBOX_TOKEN in a .env file.
 */
window.AIRPORT_PROTOTYPE_CONFIG = {
  MAPBOX_TOKEN: 'pk.YOUR_PUBLIC_TOKEN_HERE',
};
