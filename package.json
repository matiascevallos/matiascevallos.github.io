{
  "name": "matiascevallos-github-io",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build && vite preview --outDir airport-prototype --port 5173 --strictPort",
    "dev:watch": "vite build --watch",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/mapbox-gl": "^3.4.1",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "mapbox-gl": "^3.14.0"
  }
}
